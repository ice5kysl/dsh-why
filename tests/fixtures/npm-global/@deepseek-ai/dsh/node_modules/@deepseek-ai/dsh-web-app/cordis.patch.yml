# fixture: the web bundle's client rows — mounted here, NOT seed words of the
# shell (the row branch the seed table alone cannot see)
- id: ui-attachment
  name: '@deepseek-ai/dsh-client-ui-attachment'
- id: client-connection
  name: '@deepseek-ai/dsh-client-connection'
