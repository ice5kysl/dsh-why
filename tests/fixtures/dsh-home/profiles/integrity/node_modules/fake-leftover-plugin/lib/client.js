window.__ModuleLoader__.load({
  id: "fake-guarded-plugin",
  factory: (require) => {
    var module = { exports: {} };
    // two-generation host compatibility: the missing module sits inside a
    // try/catch pair, so the loader's call-time require() throw is caught —
    // NOT a boot crash, and dsh-why must say so (guard-aware R1)
    let host;
    try {
      host = require("@deepseek-ai/dsh-client-runtime/client");
    } catch (e) {
      const React = require("react");
      host = { fallback: true, React };
    }
    module.exports = { host };
    return module.exports;
  },
});
