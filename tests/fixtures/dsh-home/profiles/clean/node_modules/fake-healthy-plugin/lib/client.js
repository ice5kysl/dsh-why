window.__ModuleLoader__.load({
  id: "fake-healthy-plugin",
  factory: (require) => {
    var module = { exports: {} };
    var React = require("react");
    var jsx = require("react/jsx-runtime");
    // template-string require: statically undecidable, excluded from the set
    var lazy = require(`./chunks/${name}.js`);
    // a try/catch mention inside a string literal must not fool guard pairing
    var help = "try { this } catch { that }";
    module.exports = { React, jsx, lazy, help };
    return module.exports;
  },
});
