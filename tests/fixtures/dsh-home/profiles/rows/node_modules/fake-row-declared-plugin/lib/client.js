window.__ModuleLoader__.load({
  id: "fake-row-declared-plugin",
  factory: (require) => {
    var module = { exports: {} };
    // same lazy row as fake-row-plugin, but declared in dsh.client.inject:
    // the loader arrives it before this plugin materializes → resolves for
    // certain, so dsh-why must stay silent here.
    var { ImageGallery } = require("@deepseek-ai/dsh-client-ui-attachment");
    module.exports = { ImageGallery };
    return module.exports;
  },
});
