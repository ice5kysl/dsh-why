window.__ModuleLoader__.load({
  id: "fake-row-plugin",
  factory: (require) => {
    var module = { exports: {} };
    // immediate row + a declared inject row: both resolve for certain
    var connection = require("@deepseek-ai/dsh-client-connection");
    // LAZY built-in row the plugin did NOT declare: registered when its combo
    // batch executes, so it resolves in practice but carries no ordering
    // guarantee — conditional (warning), never a crash. Real case:
    // dsh-vision-router / @deepseek-ai/dsh-client-ui-attachment.
    var { ImageGallery } = require("@deepseek-ai/dsh-client-ui-attachment");
    module.exports = { connection, ImageGallery };
    return module.exports;
  },
});
